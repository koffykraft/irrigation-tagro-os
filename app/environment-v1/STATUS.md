WORKING / STAGING ONLY.

RUNTIME-CONFIRMED on the Cloudflare preview through build 20260824c: Worker binding/configuration, Anthropic Claude Sonnet 4.6 through AI Gateway BYOK, JSON response parsing, full conversational TAGRO adviser, deterministic engineering conformance 13/13, product knowledge retrieval, and anonymous job-local learning evidence smoke behavior.

CODED AFTER THAT CONFIRMATION and not yet claimed runtime-accepted: build 20260824d/e loads job-local learning into the real environment; injects same-job evidence into later adviser turns; records actual preference/proposal UI decisions; normalizes the three legacy Jain product-index records; adds an optional anonymous D1 persistence adapter with no users table; and adds a canonical GeoJSON spatial store shared by a real Leaflet/Geoman FIELD map and a clean DRAWING projection. Canonical geometry uses real lng/lat coordinates and measured pipe lengths. FIELD and canonical DRAWING use the same stored object IDs and geometry; edits, duplicate and delete operate on that shared state. The adviser context receives canonical map objects and real-coordinate run lengths while the old quick-sketch canvas remains explicitly preliminary/unscaled.

CURRENT HONEST BOUNDARIES: the new FIELD/DRAWING spatial pair still needs browser acceptance on desktop and phone; D1 schema/API code exists but IRRIGATION_DB is not yet bound, so cloud save must refuse rather than pretend to persist; elevation is not yet authoritative; hydraulic PASS/FAIL still requires the actual deterministic inputs beyond measured geometry; the full Jain catalogue/images are not yet normalized; installed/service outcome learning is not yet complete; the old quick canvas has not yet been retired from the shell; and no production deployment is authorized.