WORKING / STAGING ONLY.

RUNTIME-CONFIRMED on the Cloudflare preview through build 20260824c: Worker binding/configuration, Anthropic Claude Sonnet 4.6 through AI Gateway BYOK, JSON response parsing, full conversational TAGRO adviser, deterministic engineering conformance 13/13, product knowledge retrieval, and anonymous job-local learning evidence smoke behavior.

RUNTIME OBSERVATION FROM THE OWNER on the first canonical FIELD/DRAWING pair: real FIELD geometry, measured boundary area, canonical IDs and FIELD→DRAWING continuity worked, but the interaction layer was not acceptable. Existing boundary geometry intercepted new pipe drawing, the selection card was much too large, every new line opened a nuisance selection card, tools were one-shot instead of continuous, and separate FIELD/DRAWING navigation could trap the user in the preliminary shell. This pair remains available as evidence/fallback but is not the UX target.

BUILD 20260825a adds a separate unified `workbench.html` rather than patching over that failed interaction shell. It keeps the canonical GeoJSON store, real metre measurements, same IDs across FIELD/DRAWING, AI proposal store, learning evidence and deterministic engineering foundation. It deliberately reuses V16 only as behavioral raw material: persistent/continuous drawing, suppress selection while a draw mode is active, compact non-modal object inspector, FIELD/DRAWING tabs in one page, explicit Back, screen-relative Duplicate, distance/spacing mode, and preview-before-create `Layout Laterals from this Submain`. Existing FIELD/DRAWING/V16 pages are not removed.

CURRENT HONEST BOUNDARIES: `workbench.html` is statically reviewed but not yet browser-accepted on desktop/phone. V16 capabilities still to be brought into the new workbench incrementally include richer family/tie editing, batch lateral editing, device/emitter detail controls and display, family alignment/direction picking, BOM/integrity UI, workspace reset, and other useful functions that should be added without recreating V16's page shell. D1 schema/API code exists but IRRIGATION_DB is not yet bound; elevation is not yet authoritative; hydraulic PASS/FAIL still requires deterministic inputs beyond measured geometry; full Jain catalogue/images are not yet normalized; no production deployment is authorized.
