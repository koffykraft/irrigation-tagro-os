WORKING / STAGING ONLY.

RUNTIME-CONFIRMED on the Cloudflare preview only through the previously accepted 20260824c runtime: Worker binding/configuration, Anthropic Claude Sonnet 4.6 through AI Gateway BYOK, JSON response parsing, conversational TAGRO adviser, deterministic engineering conformance 13/13, product knowledge retrieval, and anonymous job-local learning smoke behavior. Later code must not inherit that runtime acceptance automatically.

CURRENT CODED REPAIR SET: build marker `co-design-environment-v1-production-repairs-20260825h` on branch `iteration-06-plain-production` adds the six-page launcher `page-set.html` and repairs the production page set around one persistent job: Information, Field, Drawing, Adviser, Design and Materials.

CORE REPAIRS CODED IN THIS SET:
- canonical network parent changes now maintain explicit `network_parent` / `feeds` relationships as well as object parent properties;
- accepted deterministic AI geometry proposals for submains/laterals preserve their explicit anchor parent relationship;
- the anonymous cloud job envelope is now v2 and includes Information state, canonical spatial state, composition and job-local learning rather than omitting customer/plot/water/site information;
- spatial, Information and learning stores catch browser storage failures and retain a memory fallback for the current session while surfacing that persistence is not confirmed;
- canonical Design and Materials projections reassert real map measurements and explicit network ties if the older in-memory renderer attempts to overwrite them;
- the permanent Field/Drawing ribbon is reduced to frequent commands while lower-frequency tools remain under All tools;
- browser zoom is no longer disabled on the primary Workbench or compatibility Field/Drawing pages;
- compatibility Field/Drawing pages are explicitly labelled references and link back to current pages;
- a common Pages control opens the six-page launcher.

NEW AUTOMATED GATES CODED:
- existing six-surface smoke gate;
- existing functional Field/Drawing/Information gate;
- production gate covering the six-page launcher, desktop + phone viewport overflow/touch targets, canonical parent persistence, accepted proposal parentage, canonical Design/Materials authority and visible Information storage failure;
- deliberate map-library failure gate proving the Workbench enters its visible safe failure state and the canonical local store still loads.

NOT YET RUNTIME-ACCEPTED: the new production repair gates and their screenshots have not been observed through the connected Actions reader in this chat, so no PASS is claimed for this build. The owner still needs to browser-test the staged page set after the staging build is available.

KNOWN NON-BLOCKING / DEFERRED WORK: the generated `workbench-v2.js` still uses two compatibility hooks supplied by `workbench-runtime-guard.js` (`lineCoords` and stale `updateInspector` naming); cloud restore/version reconciliation remains required before D1 is enabled for production; `IRRIGATION_DB` is still not bound; Adviser conversation reload persistence is not complete; full Jain catalogue/image normalization is not complete; elevation is not authoritative; hydraulic PASS/FAIL still requires the actual deterministic inputs; no production deployment is authorized.
