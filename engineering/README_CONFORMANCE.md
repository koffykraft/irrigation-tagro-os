The file `issued-conformance-v1.json` records the regression cases as data. `irrigation-engine.js` presently mirrors those values internally so it can run without an additional fetch/bundle dependency. Any future change must update both together and is invalid unless all 13 checks still reproduce the recorded expected values or an explicitly approved superseding evidence set is introduced.