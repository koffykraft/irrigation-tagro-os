(()=>{
'use strict';
const pipeRoles=['main','submain','lateral'];
const roleLabel=r=>r==='main'?'Main':r==='submain'?'Submain':r==='lateral'?'Lateral':r||'Unidentified';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const fmt=(v,d=2)=>Number.isFinite(+v)?Number(v).toFixed(d):'—';
const mm=v=>Number.isFinite(+v)?`${fmt(v,1)} m`:'—';
const money=v=>Number.isFinite(+v)?`₹${Number(v).toLocaleString('en-IN',{maximumFractionDigits:2})}`:'—';
const now=()=>new Date().toISOString();
function idOf(l){return l?.options?.tagroObjectId||(l.options.tagroObjectId='obj-'+Date.now()+'-'+Math.random().toString(36).slice(2,7))}
function roleOf(l){return l?.options?.tagroIdentityId||null}
function identityName(l){const id=roleOf(l);if(!id)return'Unidentified';const b=document.querySelector?.(`[data-tagro-id="${CSS.escape(id)}"]`);return b?b.textContent.trim():roleLabel(id)}
function acceptedElevation(l){const x=l?.options?.tagroElevation||{},mapv=Array.isArray(x.map)?x.map:[],survey=Array.isArray(x.survey)?x.survey:[];if(x.accepted==='survey'&&survey.some(Number.isFinite))return{source:'Surveyed',values:survey.slice()};if(mapv.some(Number.isFinite))return{source:'Map terrain',values:mapv.slice()};return null}
function layerRecord(l){const m=window.TAGRO_MEASURE?.measure?.(l)||null,e=l.options.tagroEngineering||null,r=roleOf(l),h=window.TAGRO_IRRIGATION_ENGINE?.hydraulic?.(l)||null,el=acceptedElevation(l);return{id:idOf(l),identity:r,name:identityName(l),kind:m?.kind||null,shape:m?.shape||l.options.tagroShape||null,measurement:m?{lengthM:m.length??null,areaM2:m.area??null,perimeterM:m.perimeter??null,bearingDeg:m.bearing??null,radiusM:m.radius??null,diameterM:m.diameter??null,position:m.point?{lat:m.point.lat,lng:m.point.lng}:null}:null,elevation:el,engineering:e?JSON.parse(JSON.stringify(e)):null,hydraulic:h&&h.ready?{maturity:h.maturity,flowLph:h.flow?.flow??null,flowSource:h.flow?.source??null,velocityMps:h.velocity??null,frictionHeadM:h.friction??null,staticRiseM:h.staticRise??null,netHeadM:h.total??null,permissibleM:h.a?.permissibleM??null,passes:h.a?.passes??null,regime:h.a?.regime??null}:null,note:l.options.tagroNote||null};}
function collect(meta={}){const layers=window.TAGRO_CAD?.getLayers?.()||[];const records=layers.map(layerRecord);return{schema:'tagro.irrigation.project-report.v1',generatedAt:now(),project:{name:meta.name||'Irrigation Project',customer:meta.customer||'',location:meta.location||'',reference:meta.reference||'',status:meta.status||'WORKING',notes:meta.notes||''},objects:records,provenance:{geometry:'TAGRO map/CAD object geometry',measurements:'Derived from accepted geometry',elevation:'Map terrain and/or surveyed override retained separately',hydraulics:window.TAGROScience?`TAGROScience ${window.TAGROScience.version}`:'Not loaded'}}}
function designRows(p){return p.objects.filter(o=>pipeRoles.includes(o.identity)).map(o=>{const e=o.engineering||{},h=o.hydraulic||{},m=o.measurement||{},el=o.elevation;return{role:roleLabel(o.identity),id:o.id,lengthM:Number.isFinite(+e.lengthOverride)&&+e.lengthOverride>0?+e.lengthOverride:m.lengthM,lengthSource:Number.isFinite(+e.lengthOverride)&&+e.lengthOverride>0?'Design override':'Map measured',material:e.material||'',sizeMm:e.mm||'',pressureClass:e.pressureClass||'',parentId:e.parentId||'',flowLph:h.flowLph??null,velocity:h.velocityMps??null,friction:h.frictionHeadM??null,staticRise:h.staticRiseM??null,netHead:h.netHeadM??null,permissible:h.permissibleM??null,passes:h.passes??null,maturity:h.maturity||'WORKING',elevationSource:el?.source||'',note:o.note||''}})}
function bomRows(p){const pipes=new Map(),emitters=new Map(),manual=[];p.objects.forEach(o=>{if(pipeRoles.includes(o.identity)){const e=o.engineering||{},m=o.measurement||{},len=Number.isFinite(+e.lengthOverride)&&+e.lengthOverride>0?+e.lengthOverride:m.lengthM;if(len>0){const key=[o.identity,e.material||'',e.mm||'',e.pressureClass||''].join('|');const x=pipes.get(key)||{section:o.identity,item:`${roleLabel(o.identity)} pipe`,spec:[e.material,e.mm?`${e.mm} mm`:'',e.pressureClass].filter(Boolean).join(' · '),unit:'m',qty:0,source:'Accepted design geometry'};x.qty+=len;pipes.set(key,x)}if(o.identity==='lateral'&&e.emitterLph){const count=+e.emitterCount>0?+e.emitterCount:(+e.emitterSpacingM>0&&len>0?Math.floor(len/+e.emitterSpacingM)+1:0);if(count>0){const key=`emitter|${e.emitterLph}`;const x=emitters.get(key)||{section:'emitter',item:'Emitter',spec:`${e.emitterLph} LPH`,unit:'nos',qty:0,source:e.emitterCount?'Entered count':'Length + spacing'};x.qty+=count;emitters.set(key,x)}}}
(o.options?.tagroBomManual||[]).forEach?.(x=>manual.push({...x,source:x.source||'Manual'}));});
const order={emitter:1,lateral:2,submain:3,main:4,accessory:5,filter:6,pump:7,labour:8,transport:9,other:10};return[...emitters.values(),...pipes.values(),...manual].sort((a,b)=>(order[a.section]||99)-(order[b.section]||99)||a.item.localeCompare(b.item))}
function htmlShell(title,body,opts={}){return`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><style>${printCss()}</style></head><body><main class="sheet">${body}</main><script>window.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-print]').forEach(b=>b.onclick=()=>window.print())})<\/script></body></html>`}
function header(p,title){return`<header class="report-head"><div><div class="brand">THUMPASSERY AGRO</div><h1>${esc(title)}</h1><div class="sub">${esc(p.project.name)}${p.project.location?' · '+esc(p.project.location):''}</div></div><div class="meta"><b>${esc(p.project.status)}</b><span>${new Date(p.generatedAt).toLocaleString()}</span></div></header><section class="project-grid"><div><span>Customer</span><b>${esc(p.project.customer||'—')}</b></div><div><span>Reference</span><b>${esc(p.project.reference||'—')}</b></div><div><span>Source</span><b>TAGRO map / accepted survey</b></div><div><span>Generated</span><b>${esc(new Date(p.generatedAt).toLocaleDateString())}</b></div></section>`}
function designHtml(p){const rows=designRows(p),warnings=[];rows.forEach(r=>{if(!r.flowLph)warnings.push(`${r.role}: design flow incomplete`);if(r.passes===false)warnings.push(`${r.role}: exceeds protected permissible length`);if(!r.elevationSource)warnings.push(`${r.role}: elevation not loaded`)});const body=`${header(p,'Irrigation Design Record')}<section><h2>Design network</h2><table><thead><tr><th>Role</th><th>Length</th><th>Pipe</th><th>Flow</th><th>Velocity</th><th>Friction</th><th>Static</th><th>Permissible</th><th>Status</th></tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td><b>${esc(r.role)}</b><small>${esc(r.lengthSource)}</small></td><td>${mm(r.lengthM)}</td><td>${esc([r.material,r.sizeMm?`${r.sizeMm} mm`:'',r.pressureClass].filter(Boolean).join(' · ')||'—')}</td><td>${r.flowLph?fmt(r.flowLph,0)+' LPH':'—'}</td><td>${r.velocity?fmt(r.velocity,2)+' m/s':'—'}</td><td>${r.friction!==null?fmt(r.friction,2)+' m':'—'}</td><td>${r.staticRise!==null?(r.staticRise>=0?'Rise ':'Fall ')+fmt(Math.abs(r.staticRise),1)+' m':'—'}</td><td>${r.permissible?fmt(r.permissible,1)+' m':'—'}</td><td>${r.passes===true?'PASS':r.passes===false?'REVIEW':esc(r.maturity)}</td></tr>`).join(''):'<tr><td colspan="9">No identified hydraulic pipes yet.</td></tr>'}</tbody></table></section><section class="two"><div><h2>Design notes</h2><p>${esc(p.project.notes||'No project note entered.')}</p></div><div><h2>Checks</h2>${warnings.length?`<ul>${warnings.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:'<p>No outstanding checks detected from currently available data.</p>'}</div></section><section><h2>Evidence & provenance</h2><p>Geometry and dimensions are read from the same accepted map/CAD objects. Surveyed elevations, when accepted, remain distinct from map-terrain estimates. Hydraulic results are derived from the protected TAGRO calculation engine and are not re-entered on this report.</p></section><footer><button data-print>Print / Save PDF</button><span>HTML export ready · ${esc(p.schema)}</span></footer>`;return htmlShell(`${p.project.name} — Design`,body)}
function bomHtml(p){const rows=bomRows(p);const incomplete=[];if(!rows.some(x=>x.section==='main'))incomplete.push('Main pipe not yet represented in BOM');if(!rows.some(x=>x.section==='submain'))incomplete.push('Submain pipe not yet represented in BOM');if(!rows.some(x=>x.section==='lateral'))incomplete.push('Lateral pipe not yet represented in BOM');incomplete.push('Fittings are not inferred until connection/fitting identities are explicitly established.');const body=`${header(p,'Bill of Materials')}<section><h2>Material schedule</h2><table><thead><tr><th>Section</th><th>Item</th><th>Specification</th><th>Qty</th><th>Unit</th><th>Source</th></tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td>${esc(roleLabel(r.section))}</td><td><b>${esc(r.item)}</b></td><td>${esc(r.spec||'—')}</td><td>${fmt(r.qty,r.unit==='m'?1:0)}</td><td>${esc(r.unit||'')}</td><td>${esc(r.source||'')}</td></tr>`).join(''):'<tr><td colspan="6">No accepted design quantities yet.</td></tr>'}</tbody></table></section><section class="two"><div><h2>BOM rule</h2><p>Quantities derive from accepted design geometry and explicit equipment data. The BOM does not invent fittings, valves, filters, pumps, labour or transport.</p></div><div><h2>Pending</h2><ul>${incomplete.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></section><footer><button data-print>Print / Save PDF</button><span>HTML export ready · ${esc(p.schema)}</span></footer>`;return htmlShell(`${p.project.name} — BOM`,body)}
function printCss(){return`@page{size:A4;margin:12mm}*{box-sizing:border-box}body{margin:0;font:12px/1.4 system-ui,-apple-system,Segoe UI,sans-serif;color:#1e211e;background:#eee}.sheet{max-width:1120px;margin:18px auto;background:#fff;padding:28px;box-shadow:0 2px 18px #0002}.report-head{display:flex;justify-content:space-between;gap:24px;border-bottom:2px solid #20231f;padding-bottom:14px}.brand{font-size:11px;font-weight:900;letter-spacing:.12em}.report-head h1{font-size:26px;margin:4px 0}.sub{color:#666}.meta{text-align:right;display:grid;align-content:start;gap:3px}.meta b{font-size:11px}.meta span{font-size:10px;color:#777}.project-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:14px 0 20px}.project-grid div{border:1px solid #ddd;padding:8px}.project-grid span,.project-grid b{display:block}.project-grid span{font-size:9px;color:#777}.project-grid b{margin-top:2px}h2{font-size:13px;margin:18px 0 7px}table{width:100%;border-collapse:collapse;font-size:10px}th,td{border:1px solid #d8dcd8;padding:6px;text-align:left;vertical-align:top}th{background:#f4f5f3;font-size:9px;text-transform:uppercase;letter-spacing:.03em}td small{display:block;color:#777;margin-top:2px}.two{display:grid;grid-template-columns:1fr 1fr;gap:18px}.two>div{min-width:0}ul{padding-left:18px}footer{margin-top:24px;padding-top:10px;border-top:1px solid #ddd;display:flex;justify-content:space-between;align-items:center;color:#777;font-size:9px}button{border:1px solid #222;background:#fff;border-radius:6px;padding:7px 10px;font-weight:750}@media print{body{background:#fff}.sheet{box-shadow:none;margin:0;padding:0}button{display:none}}@media(max-width:760px){.sheet{margin:0;padding:14px}.project-grid{grid-template-columns:1fr 1fr}.two{grid-template-columns:1fr}table{font-size:9px}}`}
function downloadHtml(html,filename){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([html],{type:'text/html;charset=utf-8'}));a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function openPrintable(html){const w=window.open('','_blank');if(!w)return false;w.document.open();w.document.write(html);w.document.close();return true}
window.TAGRO_REPORTS={version:'1.0.0',collect,designRows,bomRows,designHtml,bomHtml,downloadDesign(meta={}){const p=collect(meta);downloadHtml(designHtml(p),'TAGRO_IRRIGATION_DESIGN.html');return p},downloadBom(meta={}){const p=collect(meta);downloadHtml(bomHtml(p),'TAGRO_IRRIGATION_BOM.html');return p},printDesign(meta={}){const p=collect(meta);openPrintable(designHtml(p));return p},printBom(meta={}){const p=collect(meta);openPrintable(bomHtml(p));return p}};
})();